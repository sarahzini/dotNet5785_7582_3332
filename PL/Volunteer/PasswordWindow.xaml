<Window x:Class="PL.Volunteer.PasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="Password Window" Height="184" Width="324"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Icon="pack://application:,,,/PL;component/Images/Login.png">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <!-- Texte d'instruction stylisé -->
        <TextBlock Grid.Row="0" 
                   Margin="0,0,0,10" 
                   TextWrapping="Wrap" 
                   FontSize="7" 
                   FontWeight="Heavy" 
                   Foreground="#333333"><Run Text="Please create a secure password that meets the following requirements:"/><LineBreak/><Run Text="        • Contains at least one uppercase letter."/><LineBreak/><Run Text="        • Contains at least one numeric digit."/><LineBreak/><Run Text="Ensure your password is strong to protect your account."/></TextBlock>
        <!-- TextBox pour le mot de passe avec masquage -->
        <TextBox Grid.Row="1"
                 Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                 Name="txtPassword"
                 TextChanged="txtPassword_TextChanged"
                 Margin="0,5"/>

        <Button Grid.Row="2" 
                Content="Update"
                Click="btnUpdatePassword_Click"
                Margin="89,20,89,0"
                Padding="10,5"
                Background="#FFB22222"
                Foreground="White"
                FontWeight="Bold"/>
    </Grid>
</Window>