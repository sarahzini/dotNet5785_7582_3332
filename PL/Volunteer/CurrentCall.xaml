<Window x:Class="PL.Volunteer.CurrentCall"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="Current Call" Height="577" Width="646"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Icon="pack://application:,,,/PL;component/Images/Login.png">

    <Window.Resources>
        <Color x:Key="MDAColor">#FFDA291C</Color>
        <SolidColorBrush x:Key="MDABrush" Color="{StaticResource MDAColor}" />
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="15" />
            <Setter Property="BorderBrush" Value="{StaticResource MDABrush}" />
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Padding" Value="15" />
        </Style>
        <Style x:Key="RedTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MDABrush}" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        <Style x:Key="RedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource MDABrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="BorderBrush" Value="{StaticResource MDABrush}" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                        CornerRadius="10" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid Margin="10,10,10,15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="27*"/>
            <ColumnDefinition Width="173*"/>
        </Grid.ColumnDefinitions>
        <!-- Card Border -->

        <Border Style="{StaticResource CardStyle}" HorizontalAlignment="Left" VerticalAlignment="Center" Width="601" Height="536" Grid.ColumnSpan="2" Margin="10,0,0,0">
            <Grid Margin="0">
                <Grid Margin="0,0,0,-8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="3*" />
                    </Grid.ColumnDefinitions>

                    <!-- MDA Logo -->
                    <Image Source="pack://application:,,,/PL;component/Images/Logo.png"
                       Grid.Row="0" Grid.ColumnSpan="2" Height="40" HorizontalAlignment="Left" Margin="198,-6,0,16" Width="140" />

                    <!-- Personal Details -->
                    <StackPanel Margin="10,0,10,28" VerticalAlignment="Bottom" Grid.ColumnSpan="2" Height="441" Grid.RowSpan="2">
                        <TextBlock Text="Current Call Assign Id:" Style="{StaticResource RedTextStyle}" Height="25" />
                        <TextBox Text="{Binding CurrentVolunteer.CurrentCall.AssignId, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}" Margin="0,0,0,10" IsReadOnly="True"/>

                        <TextBlock Text="Current Call Id:" Style="{StaticResource RedTextStyle}" />
                        <TextBox Text="{Binding CurrentVolunteer.CurrentCall.CallId, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"  Margin="0,0,0,10" IsReadOnly="True"/>


                        <TextBlock Text="Type Of Call:" Style="{StaticResource RedTextStyle}" />
                        <TextBox Text="{Binding CurrentVolunteer.CurrentCall.TypeOfCall, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}" Margin="0,0,0,10" IsReadOnly="True"/>

                        <TextBlock Text="Description:" Style="{StaticResource RedTextStyle}" />
                        <TextBox Text="{Binding CurrentVolunteer.CurrentCall.Description, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}" Margin="0,0,0,10" IsReadOnly="True"/>

                        <TextBlock Text="Call Address:" Style="{StaticResource RedTextStyle}" />
                        <TextBox Text="{Binding CurrentVolunteer.CurrentCall.CallAddress, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}" Margin="0,0,0,10" IsReadOnly="True"/>

                        <TextBlock Text="Begin Time:" Style="{StaticResource RedTextStyle}" />
                        <TextBox Text="{Binding CurrentVolunteer.CurrentCall.BeginTime, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"  Margin="0,0,0,10" IsReadOnly="True"/>

                        <TextBlock Text="Max End Time:" Style="{StaticResource RedTextStyle}" />
                        <TextBox Text="{Binding CurrentVolunteer.CurrentCall.MaxEndTime, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"  Margin="0,0,0,10" IsReadOnly="True"/>

                        <TextBlock Text="Begin Action Time:" Style="{StaticResource RedTextStyle}" />
                        <TextBox Text="{Binding CurrentVolunteer.CurrentCall.BeginActionTime, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"  Margin="0,0,0,10" IsReadOnly="True"/>

                        <TextBlock Text="Volunteer Distance To Call:" Style="{StaticResource RedTextStyle}" />
                        <TextBox Text="{Binding CurrentVolunteer.CurrentCall.VolunteerDistanceToCall, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"  Margin="0,0,0,10" IsReadOnly="True"/>

                        <TextBlock Text="Status:" Style="{StaticResource RedTextStyle}" />
                        <TextBox Text="{Binding CurrentVolunteer.CurrentCall.Status, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"  Margin="0,0,0,10" IsReadOnly="True"/>

                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
