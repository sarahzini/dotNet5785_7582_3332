<Window x:Class="PL.Main.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
        Title="Login Window" SizeToContent="WidthAndHeight"
        Background="#FFF5F5F5"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" 
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid>
        <Border VerticalAlignment="Center" HorizontalAlignment="Center" Width="350" Padding="20"
                CornerRadius="0" Background="White" BorderBrush="#FFB22222" BorderThickness="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Grid.Row="0" Margin="0,0,0,20" Text="MDA Login" FontSize="24" FontWeight="Bold"
                           HorizontalAlignment="Center" Foreground="#FFB22222">
                    <TextBlock.Effect>
                        <DropShadowEffect ShadowDepth="2" BlurRadius="4" Color="Gray"/>
                    </TextBlock.Effect>
                </TextBlock>

                <!-- ID Input -->
                <StackPanel Grid.Row="1" Margin="0,0,0,10">
                    <TextBlock Margin="0,0,0,5" Text="ID:" FontWeight="Bold" Foreground="#FF333333"/>
                    <TextBox Name="IdBox" Height="30" Padding="5"
                             Text="{Binding Id, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"
                             BorderBrush="#FFB22222" BorderThickness="1" Background="#FFFFFAFA"/>
                </StackPanel>

                <!-- Password Input -->
                <StackPanel Grid.Row="2" Margin="0,0,0,20">
                    <TextBlock Margin="0,0,0,5" Text="Password:" FontWeight="Bold" Foreground="#FF333333"/>
                    <PasswordBox Height="30" Padding="5" Background="#FFFFFAFA" BorderBrush="#FFB22222" BorderThickness="1"
                           PasswordChanged="PasswordBox_PasswordChanged" />
                </StackPanel>

                <!-- Login Button -->
                <Button Grid.Row="3" Content="Login" Width="120" Height="35" FontWeight="Bold" Foreground="White"
                        Background="#FFB22222" BorderThickness="0" HorizontalAlignment="Center" Cursor="Hand" Click="btnLoginButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFE57373"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#FFAF4448"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
